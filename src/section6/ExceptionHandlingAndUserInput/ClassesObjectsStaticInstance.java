package section6.ExceptionHandlingAndUserInput;

public class ClassesObjectsStaticInstance {

    //  CLASS can be understood as a custom data type, with custom fields and methods;
    //  INSTANTIATING a class creates an OBJECT;
    //  Class acts as a template for an object;

    //  FIELDS are variables declared in a class, but only get initialized when class is instantiated; 
    //  (static fields are an exception to this rule);
    //  ATRIBUTES are variables of a created object;

    //  STATIC FIELD is initialized with the class, holding the same value across all instances (objects);
    //  INSTANCE FIELD is initialized with the object;

    //  STATIC METHOD
    //  INSTANCE METHOD


}
