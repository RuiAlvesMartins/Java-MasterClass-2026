package section17.TerminalOperations;

public class CollectorInterface {
    
    //  Oracle Documentation: https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/stream/Collector.html

    // public interface Collector<T,A,R>
    //         T:  input type (the Stream elements type);
    //         A:  mutable accumulation type, it can be:
    //                     - a single type (e.g. StringBuilder);
    //                     - a collection type (e.g. ArrayList);
    //         R:  result type (of reduction operation); it often is the same type as A;


    //  COLLECTOR Interface
    //  it is NOT a FUNCTIONAL interface!
    //  it is part of STREAMS (java.util.stream.Collector);
    //  it is a MUTABLE REDUCTION Operation!



    //  REDUCTION OPERATIONS
    //  Terminal Operations that combine a stream of elements into a single summary result;
    //  Can be a primitive, a reference type, or a collection; 



    //  COLLECTORS
    //  is a utilitary class that implements Collector;
    //  it contains a range of useful implementations;
    //  part of Streams (java.util.stream.Collector);
    


}
